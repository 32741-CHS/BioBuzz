# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# We no longer need to auto-convert third-party libraries to use AndroidX, which slowed down the build
android.enableJetifier=false

# Allow Gradle to use up to 4 GB of RAM
# Maximum ram was stepped up cause pulling in the pedro
# library increases memory usage for building by a lot.
# Also will prevent any issues when building in the future
# as we add more dependencies.
org.gradle.jvmargs=-Xmx4096M 

android.nonTransitiveRClass=false
